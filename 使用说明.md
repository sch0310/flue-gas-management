# 烟气运维标气管理系统 - 使用说明

## 🌐 系统访问地址

**首页**: [https://webview.e2b.bj10.sandbox.cloudstudio.club/?x-cs-sandbox-id=0cbe2734e53f44fc9e0294134d1cf31c&x-cs-sandbox-port=5000](https://webview.e2b.bj10.sandbox.cloudstudio.club/?x-cs-sandbox-id=0cbe2734e53f44fc9e0294134d1cf31c&x-cs-sandbox-port=5000)

**直接访问链接**（复制后在浏览器打开）：
```
https://webview.e2b.bj10.sandbox.cloudstudio.club/?x-cs-sandbox-id=0cbe2734e53f44fc9e0294134d1cf31c&x-cs-sandbox-port=5000
```

---

## 📱 使用流程

### 第一步：配置微信提醒（必须）

1. 打开首页，点击右上角 **"⚙️ 配置"**
2. 访问 [Server酱官网](https://sct.ftqq.com) 注册/登录
3. 在"发送消息"页面复制你的 **SendKey**
4. 回到配置页面，粘贴 SendKey，点击 **"💾 保存配置"**

> ✅ 配置完成后，系统会在标气到期前 45 天自动发送微信提醒

### 第二步：添加标气记录

**方式一：电脑网页填写**
1. 首页点击 **"➕ 新增标气"**
2. 选择工厂（如：新格有色金属）
3. 标气选项自动联动（如：二氧化硫、一氧化氮、氧、高纯氮）
4. 填写浓度（保留2位小数，如：50.00）
5. 选择生产日期
6. 点击 **"✅ 提交保存"**

**方式二：手机扫码填写**
1. 首页点击 **"📱 扫码填写"**
2. 用手机扫描二维码
3. 在手机上填写标气信息并提交

### 第三步：查看与管理

- **首页** 自动显示所有标气记录
- 可按 **工厂** 或 **状态** 筛选
- **状态说明**：
  - 🟢 正常：剩余天数 > 45 天
  - 🟡 即将到期：剩余天数 ≤ 45 天
  - 🔴 已过期：剩余天数 < 0

### 第四步：接收提醒

- 系统每天自动检查（需设置定时任务）
- 标气到期前 **45 天**，微信收到提醒
- 提醒内容：工厂、标气、浓度、生产日期、到期日期、剩余天数

---

## ⏰ 设置定时检查（每天自动运行）

### 方案A：使用 curl 定时请求（推荐）

**Linux/Mac** - 编辑 crontab：
```bash
crontab -e
```

添加以下行（每天上午 9:00 检查）：
```
0 9 * * * curl -s "https://webview.e2b.bj10.sandbox.cloudstudio.club/check?x-cs-sandbox-id=0cbe2734e53f44fc9e0294134d1cf31c&x-cs-sandbox-port=5000" > /dev/null 2>&1
```

**Windows** - 创建计划任务：
1. 打开"任务计划程序"
2. 创建基本任务 → 触发器：每天 → 操作：启动程序
3. 程序：`curl`
4. 参数：`"https://webview.e2b.bj10.sandbox.cloudstudio.club/check?x-cs-sandbox-id=0cbe2734e53f44fc9e0294134d1cf31c&x-cs-sandbox-port=5000"`

### 方案B：手动检查

随时访问 `/check` 页面手动触发检查：
```
https://webview.e2b.bj10.sandbox.cloudstudio.club/check?x-cs-sandbox-id=0cbe2734e53f44fc9e0294134d1cf31c&x-cs-sandbox-port=5000
```

---

## 🏭 标气清单

| 工厂 | 标气 |
|------|------|
| **新格有色金属** | 二氧化硫、一氧化氮、氧、高纯氮 |
| **华鼎铜业** | 二氧化硫、一氧化氮、氧、高纯氮 |
| **北方常铝** | 二氧化硫、一氧化氮、氧、高纯氮 |
| **还原铁入口** | 二氧化硫、一氧化氮、氧、高纯氮 |
| **还原铁出口** | 二氧化硫、一氧化氮、二氧化氮、氧、高纯氮 |
| **绿源危废** | 二氧化硫、一氧化氮、二氧化氮、一氧化碳、氯化氢、氧、高纯氮 |

---

## 🔧 技术说明

### 系统架构
- **后端**: Python Flask (端口 5000)
- **数据库**: SQLite (`flue_gas.db`)
- **提醒服务**: Server酱 (微信公众号推送)
- **二维码生成**: 动态生成，指向表单页面

### 文件结构
```
/workspace/flue-gas-management/
├── app.py              # 主程序（Flask后端 + 前端页面）
├── flue_gas.db        # SQLite数据库（自动创建）
├── config.json         # 配置文件（Server酱SendKey）
├── server.log         # 服务日志
└── README.md          # 本文档
```

### 数据库表结构
```sql
CREATE TABLE gas_standards (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    factory TEXT NOT NULL,                    -- 工厂名称
    gas_name TEXT NOT NULL,                   -- 标气名称
    concentration REAL NOT NULL,              -- 浓度（μmol/mol）
    production_date TEXT NOT NULL,            -- 生产日期（YYYY-MM-DD）
    expiry_date TEXT NOT NULL,               -- 到期日期（自动计算：生产日期+365天）
    created_at TEXT NOT NULL,                -- 创建时间
    reminder_sent INTEGER DEFAULT 0           -- 提醒是否已发送（0=未发送，1=已发送）
);
```

---

## ❓ 常见问题

### Q1: 微信提醒没收到？
**A**: 检查以下几点：
1. Server酱 SendKey 是否配置正确（访问 `/config` 查看）
2. 访问 `/check` 页面手动触发检查，查看是否有错误信息
3. 确认标气记录的 `reminder_sent` 字段是否为 0（未发送）
4. 确认标气是否真的即将到期（剩余天数 ≤ 45 天）

### Q2: 如何修改已添加的标气信息？
**A**: 目前系统不支持在线编辑，可以：
1. 删除错误记录（首页点击"删除"）
2. 重新添加正确的记录

### Q3: 数据存在哪里？如何备份？
**A**: 数据存储在 `flue_gas.db` 文件（SQLite数据库）。
- **备份**：复制 `flue_gas.db` 文件即可
- **恢复**：将备份的 `flue_gas.db` 文件覆盖回去

### Q4: 服务停止了怎么办？
**A**: 重新启动服务：
```bash
cd /workspace/flue-gas-management
nohup python3 app.py > server.log 2>&1 &
```

### Q5: 如何查看服务日志？
**A**: 
```bash
tail -f /workspace/flue-gas-management/server.log
```

### Q6: 想要在生产环境部署，需要注意什么？
**A**: 当前使用的是 Flask 开发服务器，不适合生产环境。建议：
1. 使用 Gunicorn 或 uWSGI 作为 WSGI 服务器
2. 使用 Nginx 作为反向代理
3. 配置 HTTPS
4. 设置环境变量 `FLASK_ENV=production`

---

## 📞 联系方式

如有问题，请联系系统开发者。

---

**最后更新**: 2026-05-19
**系统版本**: v1.0
